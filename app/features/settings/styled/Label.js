// @flow

import styled from 'styled-components';

export default styled.label`
    cursor: default;
    margin: 0 0.5em;
`;

export const noteLabel = styled.label`
    cursor: default;
    margin: 0 0.5em;
    font-size: 0.7em;
    padding-left: 54px;
    margin-top: -10px;
`;
